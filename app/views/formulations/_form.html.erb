<%= form_with(model: formulation, local: true) do |form| %>
  <% if formulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(formulation.errors.count, "error") %> prohibited this formulation from being saved:</h2>

      <ul>
      <% formulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :formulation_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
